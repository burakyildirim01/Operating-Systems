p2a:
1) To compile the code open terminal cd to this folder and then run the following command:
gcc p2a.c -o p2a

2) To run the code write ./p2a to the terminal.

p2b:
1) To compile the code open terminal cd to this folder and then run the following command:
gcc p2b.c -o p2b

2) To run the code write ./p2b to the terminal.
